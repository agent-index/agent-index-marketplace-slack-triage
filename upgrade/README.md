# Upgrade Scripts

This directory contains migration scripts for upgrading between collection versions.

No migrations are needed for v1.0.0 (initial release). Future versions will add migration scripts here as needed.
